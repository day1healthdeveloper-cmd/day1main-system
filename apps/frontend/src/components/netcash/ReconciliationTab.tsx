'use client';

export function ReconciliationTab() {
  return (
    <div className="p-8 text-center">
      <p className="text-gray-600">This feature requires the backend API which has been removed.</p>
      <p className="text-sm text-gray-500 mt-2">Netcash reconciliation functionality is temporarily unavailable.</p>
    </div>
  );
}
